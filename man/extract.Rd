% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{extract}
\alias{extract}
\title{Extract pattern matches from text}
\usage{
extract(
  data,
  col_name,
  regex_table,
  pattern_col = "pattern",
  id_col = NULL,
  id_filter = NULL,
  date_col = NULL,
  date_start = NULL,
  date_end = NULL,
  typo_table = NULL,
  strict = TRUE,
  remove_acronyms = FALSE,
  clean_text = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame or character vector containing the text to search.}

\item{col_name}{Column name in data frame containing text to search through.}

\item{regex_table}{A regex lookup table with at least one pattern column.}

\item{pattern_col}{Name of the regex pattern column in regex_table (default "pattern").}

\item{id_col}{Optional column in \code{data} used to filter rows before matching.}

\item{id_filter}{Optional value or vector of IDs to restrict which rows of \code{data} are matched.}

\item{date_col}{Optional column in 'data' for date filtering.}

\item{date_start}{Optional start date for filtering 'data'.}

\item{date_end}{Optional end date for filtering 'data'.}

\item{typo_table}{Optional table to fix typos in 'data'.}

\item{strict}{Logical; if TRUE, matches are treated as whole-word; if FALSE, partial matches allowed.}

\item{remove_acronyms}{Logical; if TRUE, removes all-uppercase patterns from regex_table.}

\item{clean_text}{Logical; if TRUE, applies basic text cleaning to the input before matching.}

\item{verbose}{Logical; whether to display basic progress messages.}
}
\value{
A data frame with one row per match and with all columns.
}
\description{
Uses A regex lookup to extract pattern matches from data frame.
}
