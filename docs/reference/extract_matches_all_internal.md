# Extract All matches per pattern

Internal function to extract all matches using dual-text approach.

## Usage

``` r
extract_matches_all_internal(
  text_search,
  text_raw,
  row_ids,
  patterns,
  id_col_name,
  verbose = FALSE,
  cl = NULL
)
```
